---
title: "FJP Dados"
subtitle: "População"
author:
  - name: "Coordenação de Indicadores Sociais (CIS)"
    affiliation: "Diretoria de Estatística e Informações (Direi)"
    address: "Alameda das Acácias, 70 - São Luiz"
    city: "Belo Horizonte"
    state: "Minas Gerais"
    postal_code: "31.275-150"
    url: "https://fjp.mg.gov.br/"
lang: pt
date: "`r format(Sys.Date(), '%d de %B de %Y')`"
description: "Relatório contendo uma análise descritiva e os resultados das regras de crítica dos dados da dimensão."
output:
  html_document:
    toc: true
    toc_float: true
    number_sections: false
    df_print: kable
    fig_caption: true
    table_caption: true
    embed-resources: true
    smooth-scroll: true
    toc-location: left
    toc-title: MENU
    theme: journal
editor: visual
execute:
  echo: false
  warning: false
  error: false
  message: false
params:
  data: file.xlsx
output-file: "relatorio_po.html"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
```

```{r set_param_global}

k <- 1.5 

## "Truque" para setar "k" como uma constante
#lockBinding("k", globalenv())
```

```{r import_data}

data <- openxlsx::read.xlsx(params$data)
data$ANO <- as.character(data$ANO)
```

```{r get_var_numeric}

# Obter as siglas das variáveis do tipo numérico
var_numeric <- data |> 
  dplyr::select(
    !c("CHAVE", "IBGE6", "IBGE7", "ANO") & dplyr::where(is.numeric)
  ) |> 
  colnames() |> 
  unlist()
```

```{r get_var_character}

# Obter as siglas variáveis do tipo string
var_char <- data |> 
  dplyr::select(
    !c("CHAVE", "IBGE6", "IBGE7", "ANO") & dplyr::where(is.character)
  ) |> 
  colnames() |> 
  unlist()
```

# Introdução

A **crítica de dados** refere-se ao processo de verificação e validação da integridade dos dados. Esse processo é crucial para garantir a qualidade dos dados antes de serem utilizados para análises ou publicizados. A importância da crítica de dados reside na sua capacidade de identificar valores suspeitos ou inconsistências que poderiam comprometer a confiabilidade dos resultados obtidos a partir desses dados.

# Regras de crítica

As **regras de crítica** são diretrizes ou critérios específicos estabelecidos para avaliar se os dados atendem aos padrões de qualidade desejados. Essas regras podem incluir a verificação de intervalos de valores aceitáveis, a consistência lógica entre diferentes variáveis, e a detecção de valores ausentes, entre outras. Aplicar essas regras de maneira sistemática ajuda a assegurar que os dados sejam precisos, completos e adequados para seu propósito, promovendo a confiança nas análises e nas decisões baseadas em dados.

A padronização dos nomes permite que seja possível a identificação da classificação da regra e a presença do atributo de flexibilidade. Define-se a seguinte proposta de nomenclatura para as regras de crítica:

*1 carácter indicando finalidade + 1 carácter indicando flexibilidade + Sigla da variável*

**Classificação por finalidade**

Tipo (T): refere-se a classe da variável, são realizadas verificações no sentido de identificar se os dados são numéricos, caracteres, lógicos, entre outros.

-   **TI\_**[...]: tipo do dado está de acordo com o esperado (numérico, textual ou categórico).

Validade ou Intervalo (V): refere-se aos intervalos estabelecidos matematicamente para um dado ou indicador. Verificações de valores positivos, intervalos entre 0 e 1, são exemplos de verificação de validades, além da verificação de valores ausentes;

-   **VI_NA\_**[...]: dado não tem valor ausente/*missing*;

-   **VI\_**[...]: dado é não negativo, está devidamente categorizado ou está entre o intervalo esperado (ex.: 0% e 100%).

Consistência (C): refere-se aos casos em que se verificam as relações matemáticas com outras variáveis, por exemplo, parcelas de um total não podem ser maiores do que o próprio total.

-   **CF\_[...]\_total_year**: a soma da proporção anual é igual a $\approx$ 100%.

Distribuição (D): regras de distribuição estabelecem parâmetros esperados para as estatísticas descritivas da variável como média, mediana, máximo, mínimo e regras de identificação de *outliers*;

-   **DF\_[...]\_hampel_munic**: método de identificação de um possível valor *outlier* na série histórica;

-   **DF\_[...]\_out_munic**: segundo método de identificação de um possível valor *outlier* na série histórica;

-   **DF\_[...]\_min_munic**: o valor mais recente é maior ou igual ao menor valor da série histórica;

-   **DF\_[...]\_max_munic**: o valor mais recente é menor ou igual ao maior valor da série histórica;

-   **DF\_[...]\_min_k_munic**: o valor mais recente é maior ou igual ao $\dfrac{menor}{k}$ valor da série histórica;

-   **DF\_[...]\_max_k_munic**: o valor mais recente é menor ou igual ao $maior \times k$ valor da série histórica;

-   **DF\_[...]\_med_mov_munic**: o valor atual é menor ou igual a $mediana ~móvel \times k$;

-   **DF\_[...]\_dif_munic**: o valor mais recente da primeira diferença é menor ou igual ao maior valor da primeira diferença da série histórica;

-   **DF\_[...]\_estoque_munic**: valor atual é maior ou igual ao valor imediatamente anterior.

**Classificação por flexibilidade (F ou I)**

-   Flexível (F): construída com parâmetros esperados, mas caso algum caso falhe a regra, precisa ser investigado o motivo, identificando se é o caso de um valor atípico explicado por alguma situação.

-   Inflexível (I): necessariamente precisa ser seguida, uma regra inflexível é rígida e não existe exceção a sua condição estipulada.

```{r set_year}

# Inserir aqui apenas aquelas variáveis que possuem alguma restrição 
# no ano inicial de análise devido a fatores como mudança de metodologia
# ano_base_alter <- c(
#   "MA_CRH"   = 2001,
#   "MA_CFEM"  = 2001,
#   "MA_FROTA" = 2001
# )

# Remover os comentários para utilizar este trecho caso não haja nenhuma restrição
# para nenhuma variável
ano_base_alter <- c(
 "NA" = NA_integer_
)

exclusions_years = list(
  "NA" = NA_integer_
)
```

```{r get_cat}

## Categorias variáveis dicotômicas
# cat_dic <- c(
#   "Sim",
#   "Não"
# )

## Categorias encontradas na série histórica
# cat_ogestor <- c(
#   "Departamento, assessoria, setor ou órgão similar",
#   "Não possui estrutura",
#   "Não possui estrutura para a área",
#   "Órgão da administração indireta",
#   "Secretaria em conjunto com outra política",
#   "Secretaria em conjunto com outras políticas setoriais",
#   "Secretaria exclusiva",
#   "Secretaria municipal em conjunto com outras políticas",
#   "Secretaria municipal em conjunto com outros temas",
#   "Secretaria municipal exclusiva",
#   "Setor subordinado a outra secretaria",
#   "Setor subordinado diretamente à chefia do executivo"
# )
```

```{r map_valid_desc}

# Mapear variável e descrição de sua respectiva regra de validade para ser apresentado na tabela.
desc_regra_valid <- c(
  # Casos mais comuns
  `1` = "Valor é ≥ 0",
  `2` = "Valor está dentro da categoria esperada",
  `3` = "Valor está dentro da faixa esperada (0 a 100)",
  `4` = "Valor está dentro da faixa esperada (0 a 1000)",
  `5` = "Valor está dentro da faixa esperada (0 a 10000)",
  `6` = "Valor está dentro da faixa esperada (0 a 100000)",
  # Casos especiais
  `7` = "Valor é ≥ 1", # SP_PM e SP_PMPC
  `8` = "Valor é ≥ 8", # SP_PM1
  `9` = "Valor é > 0", # PR_POPT
  
  `10` = "Valor está dentro da faixa esperada (-100 a 100)"
)

# df com o nome da regra e a respectiva descrição
desc_regra_valid_var <- data.frame(
  Regra = c(
    "VI_PO_AREA",
    "VI_PO_POPT",
    "VI_PO_MULHERTOT",
    "VI_PO_HOMEMTOT",
    "VI_PO_URB",
    "VI_PO_RUR",
    "VI_PO_RAZAOSEXO",
    "VI_PO_TXCRESC",
    "VI_PO_TXURB",
    #"VI_PO_TFT",
    "VI_PO_TBN",
    "VI_PO_TBM",
    #"VI_PO_TBMP",
    "VI_PO_PROPP0A5",
    "VI_PO_PROPP60M",
    "VI_PO_PROPP65M",
    "VI_PO_INDENVE",
    "VI_PO_RDEPT",
    "VI_PO_RDEPJ",
    "VI_PO_RDEPI",
    "VI_PO_DENSP",
    "VI_PO_TMI_0_1"#,
    #"VI_PO_TMI_0_4",
    #"VI_PO_TMI_1_4"
  )
) |> 
  dplyr::mutate(
    `Descrição` = dplyr::case_match(
      Regra,
      "VI_PO_AREA" ~ desc_regra_valid[9],
      "VI_PO_POPT" ~ desc_regra_valid[9],
      "VI_PO_MULHERTOT" ~ desc_regra_valid[9],
      "VI_PO_HOMEMTOT" ~ desc_regra_valid[9],
      "VI_PO_URB" ~ desc_regra_valid[9],
      "VI_PO_RUR" ~ desc_regra_valid[9],
      "VI_PO_RAZAOSEXO" ~ desc_regra_valid[9],
      "VI_PO_TXCRESC" ~ desc_regra_valid[10],
      "VI_PO_TXURB" ~ desc_regra_valid[3],
      #"VI_PO_TFT" ~ desc_regra_valid[1],
      "VI_PO_TBN" ~ desc_regra_valid[7],
      "VI_PO_TBM" ~ desc_regra_valid[7],
      #"VI_PO_TBMP" ~ desc_regra_valid[7],
      "VI_PO_PROPP0A5" ~ desc_regra_valid[3],
      "VI_PO_PROPP60M" ~ desc_regra_valid[3],
      "VI_PO_PROPP65M" ~ desc_regra_valid[3],
      "VI_PO_INDENVE" ~ desc_regra_valid[3],
      "VI_PO_RDEPT" ~ desc_regra_valid[3],
      "VI_PO_RDEPJ" ~ desc_regra_valid[3],
      "VI_PO_RDEPI" ~ desc_regra_valid[3],
      "VI_PO_DENSP" ~ desc_regra_valid[7],
      "VI_PO_TMI_0_1" ~ desc_regra_valid[4]#,
      #"VI_PO_TMI_0_4" ~ desc_regra_valid[4],
      #"VI_PO_TMI_1_4" ~ desc_regra_valid[4]
    )
  )
```

```{r map_consist_desc}

# Mapear variável e descrição de sua respectiva regra de consistência para ser apresentado na tabela.
# desc_regra_consist <- c(
#   `1` = "A soma da proporção é igual a 100%" # 
# )
# 
# # df com o nome da regra e a respectiva descrição
# desc_regra_consist_var <- data.frame(
#   Regra = c(
#     "CF_PO_SOMA_TOTAL"
#   )
# ) |>
#   dplyr::mutate(
#     `Descrição` = dplyr::case_match(
#       Regra,
#       "CF_SOMA_TOTAL" ~ desc_regra_consist[1]
#     )
#   )
```

## Tipo

### Todos os indicadores

```{r rules_type}
  
rules_type <- validate::validator(
  TI_PO_AREA      = is.numeric(PO_AREA),
  TI_PO_POPT      = is.numeric(PO_POPT),
  TI_PO_MULHERTOT = is.numeric(PO_MULHERTOT),
  TI_PO_HOMEMTOT  = is.numeric(PO_HOMEMTOT),
  TI_PO_URB       = is.numeric(PO_URB),
  TI_PO_RUR       = is.numeric(PO_RUR),
  TI_PO_RAZAOSEXO = is.numeric(PO_RAZAOSEXO),
  TI_PO_TXCRESC   = is.numeric(PO_TXCRESC),
  TI_PO_TXURB     = is.numeric(PO_TXURB),
  #TI_PO_TFT       = is.numeric(PO_TFT),
  TI_PO_TBN       = is.numeric(PO_TBN),
  TI_PO_TBM       = is.numeric(PO_TBM),
  #TI_PO_TBMP      = is.numeric(PO_TBMP),
  TI_PO_PROPP0A5  = is.numeric(PO_PROPP0A5),
  TI_PO_PROPP60M  = is.numeric(PO_PROPP60M),
  TI_PO_PROPP65M  = is.numeric(PO_PROPP65M),
  TI_PO_INDENVE   = is.numeric(PO_INDENVE),
  TI_PO_RDEPT     = is.numeric(PO_RDEPT),
  TI_PO_RDEPJ     = is.numeric(PO_RDEPJ),
  TI_PO_RDEPI     = is.numeric(PO_RDEPI),
  TI_PO_DENSP     = is.numeric(PO_DENSP),
  TI_PO_TMI_0_1   = is.numeric(PO_TMI_0_1)#,
  #TI_PO_TMI_0_4   = is.numeric(PO_TMI_0_4),
  #TI_PO_TMI_1_4   = is.numeric(PO_TMI_1_4)
)
```

```{r confront_type}

## Confrontar os dados com as regras e exibir uma tabela com os resultados
check_type <- validate::confront(data, rules_type)

validate::summary(check_type) |> 
  dplyr::select(
    Regra    = name,
    Validada = passes
  ) |> 
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |>
  kableExtra::kable_paper("hover", full_width = F)
```

## Validade

```{r data_rules_valid_po_all}

## df com o resultado das aplicações das regras
df_rules_valid_po_all <- data |> 
  dplyr::select(
    Ano   = ANO, 
    ibge7 = IBGE7, 
    dplyr::starts_with("PO_")
  ) |> 
  dplyr::mutate(
    # Missing
    VI_NA_PO_AREA = dplyr::if_else(!is.na(PO_AREA), F, NA),
    VI_NA_PO_POPT = dplyr::if_else(!is.na(PO_POPT), F, NA),
    VI_NA_PO_MULHERTOT = dplyr::if_else(!is.na(PO_MULHERTOT), F, NA),
    VI_NA_PO_HOMEMTOT = dplyr::if_else(!is.na(PO_HOMEMTOT), F, NA),
    VI_NA_PO_URB = dplyr::if_else(!is.na(PO_URB), F, NA),
    VI_NA_PO_RUR = dplyr::if_else(!is.na(PO_RUR), F, NA),
    VI_NA_PO_RAZAOSEXO = dplyr::if_else(!is.na(PO_RAZAOSEXO), F, NA),
    VI_NA_PO_TXCRESC = dplyr::if_else(!is.na(PO_TXCRESC), F, NA),
    VI_NA_PO_TXURB = dplyr::if_else(!is.na(PO_TXURB), F, NA),
    #VI_NA_PO_TFT = dplyr::if_else(!is.na(PO_TFT), F, NA),
    VI_NA_PO_TBN = dplyr::if_else(!is.na(PO_TBN), F, NA),
    VI_NA_PO_TBM = dplyr::if_else(!is.na(PO_TBM), F, NA),
    #VI_NA_PO_TBMP = dplyr::if_else(!is.na(PO_TBMP), F, NA),
    VI_NA_PO_PROPP0A5 = dplyr::if_else(!is.na(PO_PROPP0A5), F, NA),
    VI_NA_PO_PROPP60M = dplyr::if_else(!is.na(PO_PROPP60M), F, NA),
    VI_NA_PO_PROPP65M = dplyr::if_else(!is.na(PO_PROPP65M), F, NA),
    VI_NA_PO_INDENVE = dplyr::if_else(!is.na(PO_INDENVE), F, NA),
    VI_NA_PO_RDEPT = dplyr::if_else(!is.na(PO_RDEPT), F, NA),
    VI_NA_PO_RDEPJ = dplyr::if_else(!is.na(PO_RDEPJ), F, NA),
    VI_NA_PO_RDEPI = dplyr::if_else(!is.na(PO_RDEPI), F, NA),
    VI_NA_PO_DENSP = dplyr::if_else(!is.na(PO_DENSP), F, NA),
    VI_NA_PO_TMI_0_1 = dplyr::if_else(!is.na(PO_TMI_0_1), F, NA),
    #VI_NA_PO_TMI_0_4 = dplyr::if_else(!is.na(PO_TMI_0_4), F, NA),
    #VI_NA_PO_TMI_1_4 = dplyr::if_else(!is.na(PO_TMI_1_4), F, NA),
    # Others
    VI_PO_AREA       = dplyr::if_else(PO_AREA > 0, F, T),
    VI_PO_POPT       = dplyr::if_else(PO_POPT > 0, F, T),
    VI_PO_MULHERTOT  = dplyr::if_else(PO_MULHERTOT > 0, F, T),
    VI_PO_HOMEMTOT   = dplyr::if_else(PO_HOMEMTOT > 0, F, T),
    VI_PO_URB        = dplyr::if_else(PO_URB > 0, F, T),
    VI_PO_RUR        = dplyr::if_else(PO_RUR > 0, F, T),
    VI_PO_RAZAOSEXO  = dplyr::if_else(PO_RAZAOSEXO > 0, F, T),
    VI_PO_TXCRESC    = dplyr::if_else(dplyr::between(PO_TXCRESC, -100, 100), F, T),
    VI_PO_TXURB      = dplyr::if_else(dplyr::between(PO_TXURB, 0, 100), F, T),
    #VI_PO_TFT        = dplyr::if_else(PO_TFT >= 0, F, T),
    VI_PO_TBN        = dplyr::if_else(PO_TBN > 0 , F, T),
    VI_PO_TBM        = dplyr::if_else(PO_TBM > 0, F, T),
    #VI_PO_TBMP       = dplyr::if_else(PO_TBMP > 0, F, T),
    VI_PO_PROPP0A5   = dplyr::if_else(dplyr::between(PO_PROPP0A5, 0, 100), F, T),
    VI_PO_PROPP60M   = dplyr::if_else(dplyr::between(PO_PROPP60M, 0, 100), F, T),
    VI_PO_PROPP65M   = dplyr::if_else(dplyr::between(PO_PROPP65M, 0, 100), F, T),
    VI_PO_INDENVE    = dplyr::if_else(dplyr::between(PO_INDENVE, 0, 100), F, T),
    VI_PO_RDEPT      = dplyr::if_else(dplyr::between(PO_RDEPT, 0, 100), F, T),
    VI_PO_RDEPJ      = dplyr::if_else(dplyr::between(PO_RDEPJ, 0, 100), F, T),
    VI_PO_RDEPI      = dplyr::if_else(dplyr::between(PO_RDEPI, 0, 100), F, T),
    VI_PO_DENSP      = dplyr::if_else(PO_DENSP > 0, F, T),
    VI_PO_TMI_0_1    = dplyr::if_else(PO_TMI_0_1>= 0, F, T)#,
    #VI_PO_TMI_0_4    = dplyr::if_else(PO_TMI_0_4 >= 0, F, T),
    #VI_PO_TMI_1_4    = dplyr::if_else(PO_TMI_1_4 >=0, F, T)
  )
```

```{r data_wrangling_valid_po_all}

## Transformação dos resultados
df_sumario_valid_po <- df_rules_valid_po_all |> 
  dplyr::select(
    Ano, 
    ibge7, 
    dplyr::starts_with(c("VI_", "VF_"))
  ) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_valid_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round((Suspeita / (Total - Ausente)) * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

### Todos os indicadores - Valores ausentes

```{r table_valid_po_na_all}

df_sumario |>
  dplyr::select(Ano, Regra, Total, contains("Ausente")) |>
  dplyr::filter(
    Ano == max(Ano) & 
    stringr::str_detect(Regra, "_NA_")
  ) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

### Todos os indicadores - Demais regras

```{r table_valid_po_all}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(
    Ano == max(Ano) & 
    stringr::str_detect(Regra, "_NA_", negate = T)
  ) |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  dplyr::left_join(desc_regra_valid_var, by = dplyr::join_by(Regra)) |> # Introdução coluna descrição
  dplyr::mutate(`% Suspeita` = ifelse(Total == Ausente, 0, `% Suspeita`))|>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```



```{r table_consist_po_all}
# #| column: screen-inset-right
# 
# df_sumario |> 
#   dplyr::filter(Ano == max(Ano)) |>
#   dplyr::mutate(Ano = as.character(Ano)) |>
#   dplyr::left_join(desc_regra_consist_var, by = dplyr::join_by(Regra)) |> # Introdução coluna descrição
#   kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
#   kableExtra::kable_paper("hover", full_width = F)
```

## Distribuição

### PO_AREA

```{r data_rules_dist_po_area}

## df com o resultado das aplicações das regras
df_rules_dist_po_area <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_AREA" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_AREA"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_AREA) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_AREA_hampel_munic  = dplyr::if_else(!hampel_filter(PO_AREA), F, T), # Longitudinal (município)
    
    DF_PO_AREA_out_munic     = dplyr::if_else(!outlier_function(PO_AREA), F, T), # Longitudinal (município)
    
    DF_PO_AREA_min_munic     = dplyr::if_else(PO_AREA >= (min(dplyr::lag(PO_AREA), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_AREA_max_munic     = dplyr::if_else(PO_AREA <= (max(dplyr::lag(PO_AREA), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_AREA_min_k_munic   = dplyr::if_else(PO_AREA >= (min(dplyr::lag(PO_AREA), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_AREA_max_k_munic   = dplyr::if_else(PO_AREA <= (k * max(dplyr::lag(PO_AREA), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_AREA_med_mov_munic = dplyr::if_else(PO_AREA <= (k * zoo::rollmedian(PO_AREA, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_AREA_dif_munic     = dplyr::if_else(!compare_first_dif(PO_AREA), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_area}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_area |> 
  dplyr::select(!PO_AREA) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_area}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_area}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```




### PO_POPT

```{r data_rules_dist_po_popt}

## df com o resultado das aplicações das regras
df_rules_dist_po_popt <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_POPT" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_POPT"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_POPT) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_POPT_hampel_munic  = dplyr::if_else(!hampel_filter(PO_POPT), F, T), # Longitudinal (município)
    
    DF_PO_POPT_out_munic     = dplyr::if_else(!outlier_function(PO_POPT), F, T), # Longitudinal (município)
    
    DF_PO_POPT_min_munic     = dplyr::if_else(PO_POPT >= (min(dplyr::lag(PO_POPT), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_POPT_max_munic     = dplyr::if_else(PO_POPT <= (max(dplyr::lag(PO_POPT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_POPT_min_k_munic   = dplyr::if_else(PO_POPT >= (min(dplyr::lag(PO_POPT), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_POPT_max_k_munic   = dplyr::if_else(PO_POPT <= (k * max(dplyr::lag(PO_POPT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_POPT_med_mov_munic = dplyr::if_else(PO_POPT <= (k * zoo::rollmedian(PO_POPT, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_POPT_dif_munic     = dplyr::if_else(!compare_first_dif(PO_POPT), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_popt}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_popt |> 
  dplyr::select(!PO_POPT) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_popt}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_popt}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```





### PO_MULHERTOT

```{r data_rules_dist_po_mulhertot}

## df com o resultado das aplicações das regras
df_rules_dist_po_mulhertot <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_MULHERTOT" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_MULHERTOT"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_MULHERTOT) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_MULHERTOT_hampel_munic  = dplyr::if_else(!hampel_filter(PO_MULHERTOT), F, T), # Longitudinal (município)
    
    DF_PO_MULHERTOT_out_munic     = dplyr::if_else(!outlier_function(PO_MULHERTOT), F, T), # Longitudinal (município)
    
    DF_PO_MULHERTOT_min_munic     = dplyr::if_else(PO_MULHERTOT >= (min(dplyr::lag(PO_MULHERTOT), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_MULHERTOT_max_munic     = dplyr::if_else(PO_MULHERTOT <= (max(dplyr::lag(PO_MULHERTOT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_MULHERTOT_min_k_munic   = dplyr::if_else(PO_MULHERTOT >= (min(dplyr::lag(PO_MULHERTOT), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_MULHERTOT_max_k_munic   = dplyr::if_else(PO_MULHERTOT <= (k * max(dplyr::lag(PO_MULHERTOT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_MULHERTOT_med_mov_munic = dplyr::if_else(PO_MULHERTOT <= (k * zoo::rollmedian(PO_MULHERTOT, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_MULHERTOT_dif_munic     = dplyr::if_else(!compare_first_dif(PO_MULHERTOT), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_mulhertot}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_mulhertot |> 
  dplyr::select(!PO_MULHERTOT) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_mulhertot}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_mulhertot}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```






### PO_HOMEMTOT

```{r data_rules_dist_po_homemtot}

## df com o resultado das aplicações das regras
df_rules_dist_po_homemtot <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_HOMEMTOT" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_HOMEMTOT"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_HOMEMTOT) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_HOMEMTOT_hampel_munic  = dplyr::if_else(!hampel_filter(PO_HOMEMTOT), F, T), # Longitudinal (município)
    
    DF_PO_HOMEMTOT_out_munic     = dplyr::if_else(!outlier_function(PO_HOMEMTOT), F, T), # Longitudinal (município)
    
    DF_PO_HOMEMTOT_min_munic     = dplyr::if_else(PO_HOMEMTOT >= (min(dplyr::lag(PO_HOMEMTOT), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_HOMEMTOT_max_munic     = dplyr::if_else(PO_HOMEMTOT <= (max(dplyr::lag(PO_HOMEMTOT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_HOMEMTOT_min_k_munic   = dplyr::if_else(PO_HOMEMTOT >= (min(dplyr::lag(PO_HOMEMTOT), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_HOMEMTOT_max_k_munic   = dplyr::if_else(PO_HOMEMTOT <= (k * max(dplyr::lag(PO_HOMEMTOT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_HOMEMTOT_med_mov_munic = dplyr::if_else(PO_HOMEMTOT <= (k * zoo::rollmedian(PO_HOMEMTOT, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_HOMEMTOT_dif_munic     = dplyr::if_else(!compare_first_dif(PO_HOMEMTOT), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_homemtot}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_homemtot |> 
  dplyr::select(!PO_HOMEMTOT) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_homemtot}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_homemtot}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```









### PO_URB

```{r data_rules_dist_po_urb}

## df com o resultado das aplicações das regras
df_rules_dist_po_urb <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_URB" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_URB"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_URB) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_URB_hampel_munic  = dplyr::if_else(!hampel_filter(PO_URB), F, T), # Longitudinal (município)
    
    DF_PO_URB_out_munic     = dplyr::if_else(!outlier_function(PO_URB), F, T), # Longitudinal (município)
    
    DF_PO_URB_min_munic     = dplyr::if_else(PO_URB >= (min(dplyr::lag(PO_URB), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_URB_max_munic     = dplyr::if_else(PO_URB <= (max(dplyr::lag(PO_URB), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_URB_min_k_munic   = dplyr::if_else(PO_URB >= (min(dplyr::lag(PO_URB), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_URB_max_k_munic   = dplyr::if_else(PO_URB <= (k * max(dplyr::lag(PO_URB), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_URB_med_mov_munic = dplyr::if_else(PO_URB <= (k * zoo::rollmedian(PO_URB, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_URB_dif_munic     = dplyr::if_else(!compare_first_dif(PO_URB), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_urb}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_urb |> 
  dplyr::select(!PO_URB) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_urb}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_urb}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```













### PO_RUR

```{r data_rules_dist_po_rur}

## df com o resultado das aplicações das regras
df_rules_dist_po_rur <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_RUR" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_RUR"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_RUR) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_RUR_hampel_munic  = dplyr::if_else(!hampel_filter(PO_RUR), F, T), # Longitudinal (município)
    
    DF_PO_RUR_out_munic     = dplyr::if_else(!outlier_function(PO_RUR), F, T), # Longitudinal (município)
    
    DF_PO_RUR_min_munic     = dplyr::if_else(PO_RUR >= (min(dplyr::lag(PO_RUR), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_RUR_max_munic     = dplyr::if_else(PO_RUR <= (max(dplyr::lag(PO_RUR), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RUR_min_k_munic   = dplyr::if_else(PO_RUR >= (min(dplyr::lag(PO_RUR), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_RUR_max_k_munic   = dplyr::if_else(PO_RUR <= (k * max(dplyr::lag(PO_RUR), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RUR_med_mov_munic = dplyr::if_else(PO_RUR <= (k * zoo::rollmedian(PO_RUR, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_RUR_dif_munic     = dplyr::if_else(!compare_first_dif(PO_RUR), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_rur}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_rur |> 
  dplyr::select(!PO_RUR) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_rur}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_rur}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```















### PO_RAZAOSEXO

```{r data_rules_dist_po_razaosexo}

## df com o resultado das aplicações das regras
df_rules_dist_po_razaosexo <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_RAZAOSEXO" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_RAZAOSEXO"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_RAZAOSEXO) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_RAZAOSEXO_hampel_munic  = dplyr::if_else(!hampel_filter(PO_RAZAOSEXO), F, T), # Longitudinal (município)
    
    DF_PO_RAZAOSEXO_out_munic     = dplyr::if_else(!outlier_function(PO_RAZAOSEXO), F, T), # Longitudinal (município)
    
    DF_PO_RAZAOSEXO_min_munic     = dplyr::if_else(PO_RAZAOSEXO >= (min(dplyr::lag(PO_RAZAOSEXO), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_RAZAOSEXO_max_munic     = dplyr::if_else(PO_RAZAOSEXO <= (max(dplyr::lag(PO_RAZAOSEXO), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RAZAOSEXO_min_k_munic   = dplyr::if_else(PO_RAZAOSEXO >= (min(dplyr::lag(PO_RAZAOSEXO), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_RAZAOSEXO_max_k_munic   = dplyr::if_else(PO_RAZAOSEXO <= (k * max(dplyr::lag(PO_RAZAOSEXO), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RAZAOSEXO_med_mov_munic = dplyr::if_else(PO_RAZAOSEXO <= (k * zoo::rollmedian(PO_RAZAOSEXO, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_RAZAOSEXO_dif_munic     = dplyr::if_else(!compare_first_dif(PO_RAZAOSEXO), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_razaosexo}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_razaosexo |> 
  dplyr::select(!PO_RAZAOSEXO) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_razaosexo}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_razaosexo}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```

















### PO_TXCRESC

```{r data_rules_dist_po_txcresc}

## df com o resultado das aplicações das regras
df_rules_dist_po_txcresc <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_TXCRESC" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TXCRESC"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TXCRESC) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_TXCRESC_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TXCRESC), F, T), # Longitudinal (município)
    
    DF_PO_TXCRESC_out_munic     = dplyr::if_else(!outlier_function(PO_TXCRESC), F, T), # Longitudinal (município)
    
    DF_PO_TXCRESC_min_munic     = dplyr::if_else(PO_TXCRESC >= (min(dplyr::lag(PO_TXCRESC), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_TXCRESC_max_munic     = dplyr::if_else(PO_TXCRESC <= (max(dplyr::lag(PO_TXCRESC), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TXCRESC_min_k_munic   = dplyr::if_else(PO_TXCRESC >= (min(dplyr::lag(PO_TXCRESC), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_TXCRESC_max_k_munic   = dplyr::if_else(PO_TXCRESC <= (k * max(dplyr::lag(PO_TXCRESC), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TXCRESC_med_mov_munic = dplyr::if_else(PO_TXCRESC <= (k * zoo::rollmedian(PO_TXCRESC, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_TXCRESC_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TXCRESC), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_txcresc}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_txcresc |> 
  dplyr::select(!PO_TXCRESC) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_txcresc}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_txcresc}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```



















### PO_TXURB

```{r data_rules_dist_po_txurb}

## df com o resultado das aplicações das regras
df_rules_dist_po_txurb <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_TXURB" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TXURB"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TXURB) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_TXURB_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TXURB), F, T), # Longitudinal (município)
    
    DF_PO_TXURB_out_munic     = dplyr::if_else(!outlier_function(PO_TXURB), F, T), # Longitudinal (município)
    
    DF_PO_TXURB_min_munic     = dplyr::if_else(PO_TXURB >= (min(dplyr::lag(PO_TXURB), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_TXURB_max_munic     = dplyr::if_else(PO_TXURB <= (max(dplyr::lag(PO_TXURB), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TXURB_min_k_munic   = dplyr::if_else(PO_TXURB >= (min(dplyr::lag(PO_TXURB), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_TXURB_max_k_munic   = dplyr::if_else(PO_TXURB <= (k * max(dplyr::lag(PO_TXURB), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TXURB_med_mov_munic = dplyr::if_else(PO_TXURB <= (k * zoo::rollmedian(PO_TXURB, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_TXURB_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TXURB), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_txurb}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_txurb |> 
  dplyr::select(!PO_TXURB) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_txurb}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_txurb}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```























<!-- ### PO_TFT -->

<!-- ```{r data_rules_dist_po_tft} -->

<!-- ## df com o resultado das aplicações das regras -->
<!-- df_rules_dist_po_tft <- data |>  -->
<!--   dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter" -->
<!--     dplyr::case_when( -->
<!--       "PO_TFT" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TFT"], -->
<!--       TRUE ~ ANO >= min(ANO, na.rm = T) -->
<!--     ) -->
<!--   ) |>  -->
<!--   dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TFT) |>  -->
<!--   dplyr::group_by(ibge7) |>  -->
<!--   dplyr::mutate( -->
<!--     DF_PO_TFT_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TFT), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_out_munic     = dplyr::if_else(!outlier_function(PO_TFT), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_min_munic     = dplyr::if_else(PO_TFT >= (min(dplyr::lag(PO_TFT), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_max_munic     = dplyr::if_else(PO_TFT <= (max(dplyr::lag(PO_TFT), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_min_k_munic   = dplyr::if_else(PO_TFT >= (min(dplyr::lag(PO_TFT), na.rm = T) / k), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_max_k_munic   = dplyr::if_else(PO_TFT <= (k * max(dplyr::lag(PO_TFT), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_med_mov_munic = dplyr::if_else(PO_TFT <= (k * zoo::rollmedian(PO_TFT, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município) -->

<!--     DF_PO_TFT_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TFT), F, T) # Longitudinal (município) -->
<!-- ) -->
<!-- ``` -->

<!-- ```{r data_wrangling_dist_po_tft} -->

<!-- ## Transformação dos resultados -->
<!-- df_sumario_dist_po <- df_rules_dist_po_tft |>  -->
<!--   dplyr::select(!PO_TFT) |>  -->
<!--   tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado") -->

<!-- ## df com um sumário dos resultados -->
<!-- df_sumario <- df_sumario_dist_po |>  -->
<!--   dplyr::group_by(Ano, Regra) |>  -->
<!--   dplyr::summarise( -->
<!--     Total        = dplyr::n(), -->
<!--     Validada     = sum(!resultado, na.rm = T), -->
<!--     Suspeita     = sum(resultado, na.rm = T), -->
<!--     Ausente      = sum(is.na(resultado)), -->
<!--     `% Validada` = round(Validada / Total * 100, 2), -->
<!--     `% Suspeita` = round(Suspeita / Total * 100, 2), -->
<!--     `% Ausente`  = round(Ausente / Total * 100, 2) -->
<!--   ) |>  -->
<!--   dplyr::ungroup() -->
<!-- ``` -->

<!-- ```{r table_dist_po_tft} -->
<!-- #| column: screen-inset-right -->

<!-- df_sumario |> -->
<!--   dplyr::filter(Ano == max(Ano)) |> -->
<!--   dplyr::mutate(Ano = as.character(Ano)) |> -->
<!--   kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |>  -->
<!--   kableExtra::kable_paper("hover", full_width = F) -->
<!-- ``` -->

<!-- ```{r chart_dist_po_tft} -->
<!-- #| column: screen-inset-right -->
<!-- #| layout-ncol: 2 -->
<!-- #| out-width: 100% -->

<!-- chart_heatmap(df_sumario) -->
<!-- chart_vbar(df_sumario) -->
<!-- ``` -->



### PO_TBN

```{r data_rules_dist_po_tbn}

## df com o resultado das aplicações das regras
df_rules_dist_po_tbn <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_TBN" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TBN"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TBN) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_TBN_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TBN), F, T), # Longitudinal (município)
    
    DF_PO_TBN_out_munic     = dplyr::if_else(!outlier_function(PO_TBN), F, T), # Longitudinal (município)
    
    DF_PO_TBN_min_munic     = dplyr::if_else(PO_TBN >= (min(dplyr::lag(PO_TBN), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_TBN_max_munic     = dplyr::if_else(PO_TBN <= (max(dplyr::lag(PO_TBN), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TBN_min_k_munic   = dplyr::if_else(PO_TBN >= (min(dplyr::lag(PO_TBN), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_TBN_max_k_munic   = dplyr::if_else(PO_TBN <= (k * max(dplyr::lag(PO_TBN), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TBN_med_mov_munic = dplyr::if_else(PO_TBN <= (k * zoo::rollmedian(PO_TBN, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_TBN_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TBN), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_tbn}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_tbn |> 
  dplyr::select(!PO_TBN) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_tbn}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_tbn}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```































### PO_TBM

```{r data_rules_dist_po_tbm}

## df com o resultado das aplicações das regras
df_rules_dist_po_tbm <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_TBM" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TBM"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TBM) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_TBM_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TBM), F, T), # Longitudinal (município)
    
    DF_PO_TBM_out_munic     = dplyr::if_else(!outlier_function(PO_TBM), F, T), # Longitudinal (município)
    
    DF_PO_TBM_min_munic     = dplyr::if_else(PO_TBM >= (min(dplyr::lag(PO_TBM), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_TBM_max_munic     = dplyr::if_else(PO_TBM <= (max(dplyr::lag(PO_TBM), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TBM_min_k_munic   = dplyr::if_else(PO_TBM >= (min(dplyr::lag(PO_TBM), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_TBM_max_k_munic   = dplyr::if_else(PO_TBM <= (k * max(dplyr::lag(PO_TBM), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TBM_med_mov_munic = dplyr::if_else(PO_TBM <= (k * zoo::rollmedian(PO_TBM, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_TBM_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TBM), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_tbm}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_tbm |> 
  dplyr::select(!PO_TBM) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_tbm}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_tbm}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```



































<!-- ### PO_TBMP -->

<!-- ```{r data_rules_dist_po_tbmp} -->

<!-- ## df com o resultado das aplicações das regras -->
<!-- df_rules_dist_po_tbmp <- data |>  -->
<!--   dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter" -->
<!--     dplyr::case_when( -->
<!--       "PO_TBMP" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TBMP"], -->
<!--       TRUE ~ ANO >= min(ANO, na.rm = T) -->
<!--     ) -->
<!--   ) |>  -->
<!--   dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TBMP) |>  -->
<!--   dplyr::group_by(ibge7) |>  -->
<!--   dplyr::mutate( -->
<!--     DF_PO_TBMP_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TBMP), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_out_munic     = dplyr::if_else(!outlier_function(PO_TBMP), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_min_munic     = dplyr::if_else(PO_TBMP >= (min(dplyr::lag(PO_TBMP), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_max_munic     = dplyr::if_else(PO_TBMP <= (max(dplyr::lag(PO_TBMP), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_min_k_munic   = dplyr::if_else(PO_TBMP >= (min(dplyr::lag(PO_TBMP), na.rm = T) / k), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_max_k_munic   = dplyr::if_else(PO_TBMP <= (k * max(dplyr::lag(PO_TBMP), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_med_mov_munic = dplyr::if_else(PO_TBMP <= (k * zoo::rollmedian(PO_TBMP, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município) -->

<!--     DF_PO_TBMP_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TBMP), F, T) # Longitudinal (município) -->
<!-- ) -->
<!-- ``` -->

<!-- ```{r data_wrangling_dist_po_tbmp} -->

<!-- ## Transformação dos resultados -->
<!-- df_sumario_dist_po <- df_rules_dist_po_tbmp |>  -->
<!--   dplyr::select(!PO_TBMP) |>  -->
<!--   tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado") -->

<!-- ## df com um sumário dos resultados -->
<!-- df_sumario <- df_sumario_dist_po |>  -->
<!--   dplyr::group_by(Ano, Regra) |>  -->
<!--   dplyr::summarise( -->
<!--     Total        = dplyr::n(), -->
<!--     Validada     = sum(!resultado, na.rm = T), -->
<!--     Suspeita     = sum(resultado, na.rm = T), -->
<!--     Ausente      = sum(is.na(resultado)), -->
<!--     `% Validada` = round(Validada / Total * 100, 2), -->
<!--     `% Suspeita` = round(Suspeita / Total * 100, 2), -->
<!--     `% Ausente`  = round(Ausente / Total * 100, 2) -->
<!--   ) |>  -->
<!--   dplyr::ungroup() -->
<!-- ``` -->

<!-- ```{r table_dist_po_tbmp} -->
<!-- #| column: screen-inset-right -->

<!-- df_sumario |> -->
<!--   dplyr::filter(Ano == max(Ano)) |> -->
<!--   dplyr::mutate(Ano = as.character(Ano)) |> -->
<!--   kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |>  -->
<!--   kableExtra::kable_paper("hover", full_width = F) -->
<!-- ``` -->

<!-- ```{r chart_dist_po_tbmp} -->
<!-- #| column: screen-inset-right -->
<!-- #| layout-ncol: 2 -->
<!-- #| out-width: 100% -->

<!-- chart_heatmap(df_sumario) -->
<!-- chart_vbar(df_sumario) -->
<!-- ``` -->

### PO_PROPP0A5

```{r data_rules_dist_po_propp0a5}

## df com o resultado das aplicações das regras
df_rules_dist_po_propp0a5 <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_PROPP0A5" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_PROPP0A5"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_PROPP0A5) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_PROPP0A5_hampel_munic  = dplyr::if_else(!hampel_filter(PO_PROPP0A5), F, T), # Longitudinal (município)
    
    DF_PO_PROPP0A5_out_munic     = dplyr::if_else(!outlier_function(PO_PROPP0A5), F, T), # Longitudinal (município)
    
    DF_PO_PROPP0A5_min_munic     = dplyr::if_else(PO_PROPP0A5 >= (min(dplyr::lag(PO_PROPP0A5), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_PROPP0A5_max_munic     = dplyr::if_else(PO_PROPP0A5 <= (max(dplyr::lag(PO_PROPP0A5), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP0A5_min_k_munic   = dplyr::if_else(PO_PROPP0A5 >= (min(dplyr::lag(PO_PROPP0A5), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_PROPP0A5_max_k_munic   = dplyr::if_else(PO_PROPP0A5 <= (k * max(dplyr::lag(PO_PROPP0A5), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP0A5_med_mov_munic = dplyr::if_else(PO_PROPP0A5 <= (k * zoo::rollmedian(PO_PROPP0A5, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_PROPP0A5_dif_munic     = dplyr::if_else(!compare_first_dif(PO_PROPP0A5), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_propp0a5}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_propp0a5 |> 
  dplyr::select(!PO_PROPP0A5) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_propp0a5}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_propp0a5}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```










































### PO_PROPP60M

```{r data_rules_dist_po_propp60m}

## df com o resultado das aplicações das regras
df_rules_dist_po_propp60m <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_PROPP60M" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_PROPP60M"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_PROPP60M) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_PROPP60M_hampel_munic  = dplyr::if_else(!hampel_filter(PO_PROPP60M), F, T), # Longitudinal (município)
    
    DF_PO_PROPP60M_out_munic     = dplyr::if_else(!outlier_function(PO_PROPP60M), F, T), # Longitudinal (município)
    
    DF_PO_PROPP60M_min_munic     = dplyr::if_else(PO_PROPP60M >= (min(dplyr::lag(PO_PROPP60M), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_PROPP60M_max_munic     = dplyr::if_else(PO_PROPP60M <= (max(dplyr::lag(PO_PROPP60M), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP60M_min_k_munic   = dplyr::if_else(PO_PROPP60M >= (min(dplyr::lag(PO_PROPP60M), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_PROPP60M_max_k_munic   = dplyr::if_else(PO_PROPP60M <= (k * max(dplyr::lag(PO_PROPP60M), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP60M_med_mov_munic = dplyr::if_else(PO_PROPP60M <= (k * zoo::rollmedian(PO_PROPP60M, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_PROPP60M_dif_munic     = dplyr::if_else(!compare_first_dif(PO_PROPP60M), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_propp60m}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_propp60m |> 
  dplyr::select(!PO_PROPP60M) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_propp60m}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_propp60m}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```













































### PO_PROPP65M

```{r data_rules_dist_po_propp65m}

## df com o resultado das aplicações das regras
df_rules_dist_po_propp65m <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_PROPP65M" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_PROPP65M"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_PROPP65M) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_PROPP65M_hampel_munic  = dplyr::if_else(!hampel_filter(PO_PROPP65M), F, T), # Longitudinal (município)
    
    DF_PO_PROPP65M_out_munic     = dplyr::if_else(!outlier_function(PO_PROPP65M), F, T), # Longitudinal (município)
    
    DF_PO_PROPP65M_min_munic     = dplyr::if_else(PO_PROPP65M >= (min(dplyr::lag(PO_PROPP65M), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_PROPP65M_max_munic     = dplyr::if_else(PO_PROPP65M <= (max(dplyr::lag(PO_PROPP65M), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP65M_min_k_munic   = dplyr::if_else(PO_PROPP65M >= (min(dplyr::lag(PO_PROPP65M), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_PROPP65M_max_k_munic   = dplyr::if_else(PO_PROPP65M <= (k * max(dplyr::lag(PO_PROPP65M), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_PROPP65M_med_mov_munic = dplyr::if_else(PO_PROPP65M <= (k * zoo::rollmedian(PO_PROPP65M, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_PROPP65M_dif_munic     = dplyr::if_else(!compare_first_dif(PO_PROPP65M), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_propp65m}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_propp65m |> 
  dplyr::select(!PO_PROPP65M) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_propp65m}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_propp65m}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```


















































### PO_INDENVE

```{r data_rules_dist_po_indenve}

## df com o resultado das aplicações das regras
df_rules_dist_po_indenve <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_INDENVE" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_INDENVE"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_INDENVE) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_INDENVE_hampel_munic  = dplyr::if_else(!hampel_filter(PO_INDENVE), F, T), # Longitudinal (município)
    
    DF_PO_INDENVE_out_munic     = dplyr::if_else(!outlier_function(PO_INDENVE), F, T), # Longitudinal (município)
    
    DF_PO_INDENVE_min_munic     = dplyr::if_else(PO_INDENVE >= (min(dplyr::lag(PO_INDENVE), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_INDENVE_max_munic     = dplyr::if_else(PO_INDENVE <= (max(dplyr::lag(PO_INDENVE), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_INDENVE_min_k_munic   = dplyr::if_else(PO_INDENVE >= (min(dplyr::lag(PO_INDENVE), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_INDENVE_max_k_munic   = dplyr::if_else(PO_INDENVE <= (k * max(dplyr::lag(PO_INDENVE), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_INDENVE_med_mov_munic = dplyr::if_else(PO_INDENVE <= (k * zoo::rollmedian(PO_INDENVE, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_INDENVE_dif_munic     = dplyr::if_else(!compare_first_dif(PO_INDENVE), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_indenve}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_indenve |> 
  dplyr::select(!PO_INDENVE) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_indenve}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_indenve}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```






















































### PO_RDEPT

```{r data_rules_dist_po_rdept}

## df com o resultado das aplicações das regras
df_rules_dist_po_rdept <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_RDEPT" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_RDEPT"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_RDEPT) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_RDEPT_hampel_munic  = dplyr::if_else(!hampel_filter(PO_RDEPT), F, T), # Longitudinal (município)
    
    DF_PO_RDEPT_out_munic     = dplyr::if_else(!outlier_function(PO_RDEPT), F, T), # Longitudinal (município)
    
    DF_PO_RDEPT_min_munic     = dplyr::if_else(PO_RDEPT >= (min(dplyr::lag(PO_RDEPT), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_RDEPT_max_munic     = dplyr::if_else(PO_RDEPT <= (max(dplyr::lag(PO_RDEPT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPT_min_k_munic   = dplyr::if_else(PO_RDEPT >= (min(dplyr::lag(PO_RDEPT), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_RDEPT_max_k_munic   = dplyr::if_else(PO_RDEPT <= (k * max(dplyr::lag(PO_RDEPT), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPT_med_mov_munic = dplyr::if_else(PO_RDEPT <= (k * zoo::rollmedian(PO_RDEPT, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_RDEPT_dif_munic     = dplyr::if_else(!compare_first_dif(PO_RDEPT), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_rdept}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_rdept |> 
  dplyr::select(!PO_RDEPT) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_rdept}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_rdept}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```



























































### PO_RDEPJ

```{r data_rules_dist_po_rdepj}

## df com o resultado das aplicações das regras
df_rules_dist_po_rdepj <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_RDEPJ" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_RDEPJ"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_RDEPJ) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_RDEPJ_hampel_munic  = dplyr::if_else(!hampel_filter(PO_RDEPJ), F, T), # Longitudinal (município)
    
    DF_PO_RDEPJ_out_munic     = dplyr::if_else(!outlier_function(PO_RDEPJ), F, T), # Longitudinal (município)
    
    DF_PO_RDEPJ_min_munic     = dplyr::if_else(PO_RDEPJ >= (min(dplyr::lag(PO_RDEPJ), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_RDEPJ_max_munic     = dplyr::if_else(PO_RDEPJ <= (max(dplyr::lag(PO_RDEPJ), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPJ_min_k_munic   = dplyr::if_else(PO_RDEPJ >= (min(dplyr::lag(PO_RDEPJ), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_RDEPJ_max_k_munic   = dplyr::if_else(PO_RDEPJ <= (k * max(dplyr::lag(PO_RDEPJ), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPJ_med_mov_munic = dplyr::if_else(PO_RDEPJ <= (k * zoo::rollmedian(PO_RDEPJ, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_RDEPJ_dif_munic     = dplyr::if_else(!compare_first_dif(PO_RDEPJ), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_rdepj}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_rdepj |> 
  dplyr::select(!PO_RDEPJ) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_rdepj}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_rdepj}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```
































































### PO_RDEPI

```{r data_rules_dist_po_rdepi}

## df com o resultado das aplicações das regras
df_rules_dist_po_rdepi <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_RDEPI" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_RDEPI"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_RDEPI) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_RDEPI_hampel_munic  = dplyr::if_else(!hampel_filter(PO_RDEPI), F, T), # Longitudinal (município)
    
    DF_PO_RDEPI_out_munic     = dplyr::if_else(!outlier_function(PO_RDEPI), F, T), # Longitudinal (município)
    
    DF_PO_RDEPI_min_munic     = dplyr::if_else(PO_RDEPI >= (min(dplyr::lag(PO_RDEPI), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_RDEPI_max_munic     = dplyr::if_else(PO_RDEPI <= (max(dplyr::lag(PO_RDEPI), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPI_min_k_munic   = dplyr::if_else(PO_RDEPI >= (min(dplyr::lag(PO_RDEPI), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_RDEPI_max_k_munic   = dplyr::if_else(PO_RDEPI <= (k * max(dplyr::lag(PO_RDEPI), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_RDEPI_med_mov_munic = dplyr::if_else(PO_RDEPI <= (k * zoo::rollmedian(PO_RDEPI, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_RDEPI_dif_munic     = dplyr::if_else(!compare_first_dif(PO_RDEPI), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_rdepi}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_rdepi |> 
  dplyr::select(!PO_RDEPI) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_rdepi}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_rdepi}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```





































































### PO_DENSP

```{r data_rules_dist_po_densp}

## df com o resultado das aplicações das regras
df_rules_dist_po_densp <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_DENSP" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_DENSP"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_DENSP) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_DENSP_hampel_munic  = dplyr::if_else(!hampel_filter(PO_DENSP), F, T), # Longitudinal (município)
    
    DF_PO_DENSP_out_munic     = dplyr::if_else(!outlier_function(PO_DENSP), F, T), # Longitudinal (município)
    
    DF_PO_DENSP_min_munic     = dplyr::if_else(PO_DENSP >= (min(dplyr::lag(PO_DENSP), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_DENSP_max_munic     = dplyr::if_else(PO_DENSP <= (max(dplyr::lag(PO_DENSP), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_DENSP_min_k_munic   = dplyr::if_else(PO_DENSP >= (min(dplyr::lag(PO_DENSP), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_DENSP_max_k_munic   = dplyr::if_else(PO_DENSP <= (k * max(dplyr::lag(PO_DENSP), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_DENSP_med_mov_munic = dplyr::if_else(PO_DENSP <= (k * zoo::rollmedian(PO_DENSP, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_DENSP_dif_munic     = dplyr::if_else(!compare_first_dif(PO_DENSP), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_densp}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_densp |> 
  dplyr::select(!PO_DENSP) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_densp}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_densp}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```










































































### PO_TMI_0_1

```{r data_rules_dist_po_tmi_0_1}

## df com o resultado das aplicações das regras
df_rules_dist_po_tmi_0_1 <- data |> 
  dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter"
    dplyr::case_when(
      "PO_TMI_0_1" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TMI_0_1"],
      TRUE ~ ANO >= min(ANO, na.rm = T)
    )
  ) |> 
  dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TMI_0_1) |> 
  dplyr::group_by(ibge7) |> 
  dplyr::mutate(
    DF_PO_TMI_0_1_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TMI_0_1), F, T), # Longitudinal (município)
    
    DF_PO_TMI_0_1_out_munic     = dplyr::if_else(!outlier_function(PO_TMI_0_1), F, T), # Longitudinal (município)
    
    DF_PO_TMI_0_1_min_munic     = dplyr::if_else(PO_TMI_0_1 >= (min(dplyr::lag(PO_TMI_0_1), na.rm = T)), F, T), # Longitudinal (município)
     
    DF_PO_TMI_0_1_max_munic     = dplyr::if_else(PO_TMI_0_1 <= (max(dplyr::lag(PO_TMI_0_1), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TMI_0_1_min_k_munic   = dplyr::if_else(PO_TMI_0_1 >= (min(dplyr::lag(PO_TMI_0_1), na.rm = T) / k), F, T), # Longitudinal (município)
     
    DF_PO_TMI_0_1_max_k_munic   = dplyr::if_else(PO_TMI_0_1 <= (k * max(dplyr::lag(PO_TMI_0_1), na.rm = T)), F, T), # Longitudinal (município)
    
    DF_PO_TMI_0_1_med_mov_munic = dplyr::if_else(PO_TMI_0_1 <= (k * zoo::rollmedian(PO_TMI_0_1, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município)
    
    DF_PO_TMI_0_1_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TMI_0_1), F, T) # Longitudinal (município)
)
```

```{r data_wrangling_dist_po_tmi_0_1}

## Transformação dos resultados
df_sumario_dist_po <- df_rules_dist_po_tmi_0_1 |> 
  dplyr::select(!PO_TMI_0_1) |> 
  tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado")

## df com um sumário dos resultados
df_sumario <- df_sumario_dist_po |> 
  dplyr::group_by(Ano, Regra) |> 
  dplyr::summarise(
    Total        = dplyr::n(),
    Validada     = sum(!resultado, na.rm = T),
    Suspeita     = sum(resultado, na.rm = T),
    Ausente      = sum(is.na(resultado)),
    `% Validada` = round(Validada / Total * 100, 2),
    `% Suspeita` = round(Suspeita / Total * 100, 2),
    `% Ausente`  = round(Ausente / Total * 100, 2)
  ) |> 
  dplyr::ungroup()
```

```{r table_dist_po_tmi_0_1}
#| column: screen-inset-right

df_sumario |>
  dplyr::filter(Ano == max(Ano)) |>
  dplyr::mutate(Ano = as.character(Ano)) |>
  kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |> 
  kableExtra::kable_paper("hover", full_width = F)
```

```{r chart_dist_po_tmi_0_1}
#| column: screen-inset-right
#| layout-ncol: 2
#| out-width: 100%

chart_heatmap(df_sumario)
chart_vbar(df_sumario)
```














































































<!-- ### PO_TMI_0_4 -->

<!-- ```{r data_rules_dist_po_tmi_0_4} -->

<!-- ## df com o resultado das aplicações das regras -->
<!-- df_rules_dist_po_tmi_0_4 <- data |>  -->
<!--   dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter" -->
<!--     dplyr::case_when( -->
<!--       "PO_TMI_0_4" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TMI_0_4"], -->
<!--       TRUE ~ ANO >= min(ANO, na.rm = T) -->
<!--     ) -->
<!--   ) |>  -->
<!--   dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TMI_0_4) |>  -->
<!--   dplyr::group_by(ibge7) |>  -->
<!--   dplyr::mutate( -->
<!--     DF_PO_TMI_0_4_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TMI_0_4), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_out_munic     = dplyr::if_else(!outlier_function(PO_TMI_0_4), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_min_munic     = dplyr::if_else(PO_TMI_0_4 >= (min(dplyr::lag(PO_TMI_0_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_max_munic     = dplyr::if_else(PO_TMI_0_4 <= (max(dplyr::lag(PO_TMI_0_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_min_k_munic   = dplyr::if_else(PO_TMI_0_4 >= (min(dplyr::lag(PO_TMI_0_4), na.rm = T) / k), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_max_k_munic   = dplyr::if_else(PO_TMI_0_4 <= (k * max(dplyr::lag(PO_TMI_0_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_med_mov_munic = dplyr::if_else(PO_TMI_0_4 <= (k * zoo::rollmedian(PO_TMI_0_4, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_0_4_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TMI_0_4), F, T) # Longitudinal (município) -->
<!-- ) -->
<!-- ``` -->

<!-- ```{r data_wrangling_dist_po_tmi_0_4} -->

<!-- ## Transformação dos resultados -->
<!-- df_sumario_dist_po <- df_rules_dist_po_tmi_0_4 |>  -->
<!--   dplyr::select(!PO_TMI_0_4) |>  -->
<!--   tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado") -->

<!-- ## df com um sumário dos resultados -->
<!-- df_sumario <- df_sumario_dist_po |>  -->
<!--   dplyr::group_by(Ano, Regra) |>  -->
<!--   dplyr::summarise( -->
<!--     Total        = dplyr::n(), -->
<!--     Validada     = sum(!resultado, na.rm = T), -->
<!--     Suspeita     = sum(resultado, na.rm = T), -->
<!--     Ausente      = sum(is.na(resultado)), -->
<!--     `% Validada` = round(Validada / Total * 100, 2), -->
<!--     `% Suspeita` = round(Suspeita / Total * 100, 2), -->
<!--     `% Ausente`  = round(Ausente / Total * 100, 2) -->
<!--   ) |>  -->
<!--   dplyr::ungroup() -->
<!-- ``` -->

<!-- ```{r table_dist_po_tmi_0_4} -->
<!-- #| column: screen-inset-right -->

<!-- df_sumario |> -->
<!--   dplyr::filter(Ano == max(Ano)) |> -->
<!--   dplyr::mutate(Ano = as.character(Ano)) |> -->
<!--   kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |>  -->
<!--   kableExtra::kable_paper("hover", full_width = F) -->
<!-- ``` -->

<!-- ```{r chart_dist_po_tmi_0_4} -->
<!-- #| column: screen-inset-right -->
<!-- #| layout-ncol: 2 -->
<!-- #| out-width: 100% -->

<!-- chart_heatmap(df_sumario) -->
<!-- chart_vbar(df_sumario) -->
<!-- ``` -->


<!-- ### PO_TMI_1_4 -->

<!-- ```{r data_rules_dist_po_tmi_1_4} -->

<!-- ## df com o resultado das aplicações das regras -->
<!-- df_rules_dist_po_tmi_1_4 <- data |>  -->
<!--   dplyr::filter( # Filtro condicional para variáveis que estão em "ano_base_alter" -->
<!--     dplyr::case_when( -->
<!--       "PO_TMI_1_4" %in% names(ano_base_alter) ~ ANO >= ano_base_alter["PO_TMI_1_4"], -->
<!--       TRUE ~ ANO >= min(ANO, na.rm = T) -->
<!--     ) -->
<!--   ) |>  -->
<!--   dplyr::select(Ano = ANO, ibge7 = IBGE7, PO_TMI_1_4) |>  -->
<!--   dplyr::group_by(ibge7) |>  -->
<!--   dplyr::mutate( -->
<!--     DF_PO_TMI_1_4_hampel_munic  = dplyr::if_else(!hampel_filter(PO_TMI_1_4), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_out_munic     = dplyr::if_else(!outlier_function(PO_TMI_1_4), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_min_munic     = dplyr::if_else(PO_TMI_1_4 >= (min(dplyr::lag(PO_TMI_1_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_max_munic     = dplyr::if_else(PO_TMI_1_4 <= (max(dplyr::lag(PO_TMI_1_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_min_k_munic   = dplyr::if_else(PO_TMI_1_4 >= (min(dplyr::lag(PO_TMI_1_4), na.rm = T) / k), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_max_k_munic   = dplyr::if_else(PO_TMI_1_4 <= (k * max(dplyr::lag(PO_TMI_1_4), na.rm = T)), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_med_mov_munic = dplyr::if_else(PO_TMI_1_4 <= (k * zoo::rollmedian(PO_TMI_1_4, k = 3, fill = NA, align = "right")), F, T), # Longitudinal (município) -->

<!--     DF_PO_TMI_1_4_dif_munic     = dplyr::if_else(!compare_first_dif(PO_TMI_1_4), F, T) # Longitudinal (município) -->
<!-- ) -->
<!-- ``` -->

<!-- ```{r data_wrangling_dist_po_tmi_1_4} -->

<!-- ## Transformação dos resultados -->
<!-- df_sumario_dist_po <- df_rules_dist_po_tmi_1_4 |>  -->
<!--   dplyr::select(!PO_TMI_1_4) |>  -->
<!--   tidyr::pivot_longer(cols = !c(Ano, ibge7), names_to = "Regra", values_to = "resultado") -->

<!-- ## df com um sumário dos resultados -->
<!-- df_sumario <- df_sumario_dist_po |>  -->
<!--   dplyr::group_by(Ano, Regra) |>  -->
<!--   dplyr::summarise( -->
<!--     Total        = dplyr::n(), -->
<!--     Validada     = sum(!resultado, na.rm = T), -->
<!--     Suspeita     = sum(resultado, na.rm = T), -->
<!--     Ausente      = sum(is.na(resultado)), -->
<!--     `% Validada` = round(Validada / Total * 100, 2), -->
<!--     `% Suspeita` = round(Suspeita / Total * 100, 2), -->
<!--     `% Ausente`  = round(Ausente / Total * 100, 2) -->
<!--   ) |>  -->
<!--   dplyr::ungroup() -->
<!-- ``` -->

<!-- ```{r table_dist_po_tmi_1_4} -->
<!-- #| column: screen-inset-right -->

<!-- df_sumario |> -->
<!--   dplyr::filter(Ano == max(Ano)) |> -->
<!--   dplyr::mutate(Ano = as.character(Ano)) |> -->
<!--   kableExtra::kbl(format.args = list(big.mark = ".", decimal.mark = ",")) |>  -->
<!--   kableExtra::kable_paper("hover", full_width = F) -->
<!-- ``` -->

<!-- ```{r chart_dist_po_tmi_1_4} -->
<!-- #| column: screen-inset-right -->
<!-- #| layout-ncol: 2 -->
<!-- #| out-width: 100% -->

<!-- chart_heatmap(df_sumario) -->
<!-- chart_vbar(df_sumario) -->
<!-- ``` -->


# Análise descritiva

As tabelas a seguir apresentam um resumo descritivo dos indicadores analisados, fornecendo uma visão abrangente das características dos dados. Os cálculos foram realizados em função do ano. As informações inclusas abrangem:

-   **Quantidade de Valores (N):** Indica o número total de observações presentes no conjunto de dados para cada indicador.

-   **Valor Mínimo:** Representa o menor valor observado para o indicador.

-   **Valor da Média:** Indica a média aritmética dos valores do indicador, fornecendo uma medida central da tendência dos dados.

-   **Valor da Mediana:** Representa o valor que divide o conjunto de dados ordenados em duas metades de igual tamanho, sendo útil para lidar com casos com *outliers*.

-   **Valor Máximo:** Indica o maior valor observado para o indicador.

-   **Desvio Padrão (D.P.):** Mede a dispersão dos dados em relação à média, quantificando a variabilidade presente no conjunto.

-   **Coeficiente de Variação (C.V.):** Expressa a razão entre o desvio padrão e a média, em porcentagem, fornecendo uma medida relativa da dispersão dos dados em relação à média.

-   **Quantidade de Zeros:** Indica o número de observações com valor igual a zero para o indicador.

-   **Quantidade de Dados Ausentes:** Representa o número de observações com valores ausentes para o indicador.

**Observações Importantes:**

-   A análise de *outliers* (valores atípicos) não foi realizada nesta tabela descritiva. Para uma avaliação mais completa da qualidade dos dados, recomenda-se a análise de *outliers* e a verificação de possíveis inconsistências nos valores apresentados anteriormente neste documento.

-   A interpretação das medidas descritivas deve ser feita em conjunto com o conhecimento do contexto dos indicadores e dos objetivos da análise.

Ao analisar esta tabela, é possível obter uma compreensão inicial das características dos dados, identificando padrões, tendências e possíveis discrepâncias. As informações aqui apresentadas servem como base para análises estatísticas mais aprofundadas, auxiliando na tomada de decisões e na formulação de conclusões relevantes.

## Tabelas

```{r show_desc_table, results='asis'}
#| column: screen-inset-right

pander::pandoc.header("Descritiva", level = 3)

# Loop para mostrar os resultados por variável
for(var in var_numeric){
  pander::pandoc.header(var, level = 4)
  pander::pandoc.table(
    table_descriptive(variable = var, data = data, ano_base_alter), 
    decimal.mark = ",", 
    big.mark     = ".", 
    round        = 2,
    split.table  = Inf
  )
  cat("\\pagebreak\n")
}
```

## Gráficos

::: {.callout-note appearance="minimal"}
Observação: os gráficos abaixo são mostrados na escala logarítmica para facilitar a visualização dos dados em certos casos. Para conferir os valores reais, consultar as tabelas descritivas.
:::

```{r show_boxplot_chart, results='asis'}
#| out-width: 100%
#| fig-format: svg

pander::pandoc.header("Gráfico de caixa (boxplot)", level = 3)

# Loop para mostrar os resultados por variável
for(var in var_numeric){
  pander::pandoc.header(var, level = 4)
  data_aux <- data |> remove_year_variable(variable = var, exclusions_years = exclusions_years)
  print(chart_boxplot(variable = var, data = data_aux, ano_base_alter))
  cat("\\pagebreak\n")
}
```

```{r show_hist_chart, results='asis'}
#| out-width: 100%
#| fig-format: svg

pander::pandoc.header("Histograma", level = 3)

# Loop para mostrar os resultados por variável
for(var in var_numeric){
  pander::pandoc.header(var, level = 4)
  print(chart_histogram(variable = var, data = data, ano_base_alter))
  cat("\\pagebreak\n")
}
```

```{r excel_create_wb}

## Criar pasta de trabalho
pt <- openxlsx::createWorkbook()
path_data <- system.file("data", "cidades.xlsx", package = "dataeditfjp")
cidades <- openxlsx::read.xlsx(path_data)

```

```{r df_build_viol_valid}

variables = c(
  "PO_AREA",
  "PO_POPT",
  "PO_MULHERTOT",
  "PO_HOMEMTOT",
  "PO_URB",
  "PO_RUR",
  "PO_RAZAOSEXO",
  "PO_TXCRESC",
  "PO_TXURB",
#  "PO_TFT",
  "PO_TBN",
  "PO_TBM",
#  "PO_TBMP",
  "PO_PROPP0A5",
  "PO_PROPP60M",
  "PO_PROPP65M",
  "PO_INDENVE",
  "PO_RDEPT",
  "PO_RDEPJ",
  "PO_RDEPI",
  "PO_DENSP",
  "PO_TMI_0_1"#,
#  "PO_TMI_0_4",
#  "PO_TMI_1_4"
  )

lis <- split(variables,f = variables)
df_obs_valid <- names(lis)|>
sapply(function(i){
lis[[i]] <- df_rules_valid_po_all |>
  dplyr::select(
    Ano,
    ibge7,
    dplyr::ends_with(i)
  ) |>
  tidyr::pivot_longer(
    cols      = !c(Ano, ibge7, dplyr::starts_with("PO_")),
    names_to  = "regra",
    values_to = "obs_suspeita"
  ) |>
  dplyr::filter(obs_suspeita) |>
  dplyr::rename_with(tolower) |>
  dplyr::left_join(cidades, by = join_by(ibge7==id)) |>
  dplyr::select(ano, ibge7, cidade, obs_suspeita, regra) |>
  dplyr::arrange(ano, ibge7)
return(lis[[i]])
}, USE.NAMES=TRUE, simplify = FALSE)

index <- sapply(df_obs_valid, nrow)!=0
df_obs_valid <- df_obs_valid[index]

for(i in names(df_obs_valid)){
  openxlsx::addWorksheet(pt, paste0("valid - ", i))
  openxlsx::writeData(pt, paste0("valid - ", i), df_obs_valid[[i]])
}

```

```{r df_build_viol_dist}
df_dist <- list(
  "PO_AREA"        = df_rules_dist_po_area,
  "PO_POPT"        = df_rules_dist_po_popt,
  "PO_MULHERTOT"   = df_rules_dist_po_mulhertot,
  "PO_HOMEMTOT"    = df_rules_dist_po_homemtot,
  "PO_URB"         = df_rules_dist_po_urb, 
  "PO_RUR"         = df_rules_dist_po_rur,
  "PO_RAZAOSEXO"   = df_rules_dist_po_razaosexo,
  "PO_TXCRESC"     = df_rules_dist_po_txcresc,
  "PO_TXURB"       = df_rules_dist_po_txurb,
#  "PO_TFT"         = df_rules_dist_po_tft,
  "PO_TBN"         = df_rules_dist_po_tbn,
  "PO_TBM"         = df_rules_dist_po_tbm,
#  "PO_TBMP"        = df_rules_dist_po_tbmp,
  "PO_PROPP0A5"    = df_rules_dist_po_propp0a5,
  "PO_PROPP60M"    = df_rules_dist_po_propp60m,
  "PO_PROPP65M"    = df_rules_dist_po_propp65m,
  "PO_INDENVE"     = df_rules_dist_po_indenve,
  "PO_RDEPT"       = df_rules_dist_po_rdept,
  "PO_RDEPJ"       = df_rules_dist_po_rdepj,
  "PO_RDEPI"       = df_rules_dist_po_rdepi,
  "PO_DENSP"       = df_rules_dist_po_densp,
  "PO_TMI_0_1"     = df_rules_dist_po_tmi_0_1#,
#  "PO_TMI_0_4"     = df_rules_dist_po_tmi_0_4,
#  "PO_TMI_1_4"     = df_rules_dist_po_tmi_1_4
)

df_obs_dist <- names(df_dist)|>
sapply(function(i){
df_dist[[i]] <- df_dist[[i]] |>
  tidyr::pivot_longer(
    cols      = !c(Ano, ibge7, dplyr::starts_with("PO_")),
    names_to  = "regra",
    values_to = "obs_suspeita"
  ) |>
  dplyr::filter(obs_suspeita) |>
  dplyr::rename_with(tolower) |> 
  dplyr::left_join(cidades, by = join_by(ibge7==id)) |>
  dplyr::select(-obs_suspeita) |>
  dplyr::arrange(ano, ibge7)
return(df_dist[[i]])
}, USE.NAMES=TRUE, simplify = FALSE)


for(i in names(df_obs_dist)){
  openxlsx::addWorksheet(pt, paste0("distrib - ", i))
  openxlsx::writeData(pt, paste0("distrib - ", i), df_obs_dist[[i]])
}

```

```{r excel_save_wb}

openxlsx::saveWorkbook(pt, "fjpdados_populacao_violacoes.xlsx", overwrite = T)
```

```{r clean_env, results='hide'}
rm(list = ls())
gc()
```
